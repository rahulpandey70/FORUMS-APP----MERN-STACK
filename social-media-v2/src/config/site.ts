export type SiteConfig = {
	name: string;
	description: string;
};

export const siteConfig: SiteConfig = {
	name: "Social Hub",
	description:
		"This is a social application, where you can share your thoughts and also share images.",
};
